<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    keyframes: {
                        'fade-in': {
                            '0%': { opacity: '0', transform: 'translateY(-10px)' },
                            '100%': { opacity: '1', transform: 'translateY(0)' }
                        },
                        'pulse-glow': {
                            '0%, 100%': { transform: 'scale(1)', opacity: '1' },
                            '50%': { transform: 'scale(1.05)', opacity: '0.8' }
                        }
                    },
                    animation: {
                        'fade-in': 'fade-in 0.5s ease-out',
                        'pulse-glow': 'pulse-glow 3s ease-in-out infinite'
                    }
                }
            }
        }
    </script>
</head>

<body class="font-sans bg-gray-100 text-gray-900">
  
    <% if (typeof showHeader === "undefined" || showHeader) { %>
        <nav class="w-full flex items-center justify-between px-6 py-4 bg-gradient-to-r from-white to-blue-50 shadow-sm sticky top-0 z-50 backdrop-blur-sm border-b border-blue-100 animate-fade-in">
            <div class="flex items-center space-x-2 group">
                <div class="relative">
                    <div class="absolute -inset-1 bg-blue-200 rounded-full blur opacity-75 group-hover:opacity-100 transition-opacity duration-300"></div>
                    <svg class="w-8 h-8 text-blue-600 relative transform group-hover:rotate-12 transition-transform duration-300" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 0l-3 3a1 1 0 001.414 1.414L9 9.414V13a1 1 0 102 0V9.414l1.293 1.293a1 1 0 001.414-1.414z" clip-rule="evenodd"></path>
                    </svg>
                </div>
                <h3 class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-blue-400 bg-clip-text text-transparent animate-pulse-glow">Scatch</h3>
            </div>
        
            <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
            <% if (loggedin) { %>
                <div class="flex items-center gap-6">
                    <a href="/shop" class="relative text-gray-700 hover:text-blue-600 transition-all duration-300 group">
                        <i class="ri-store-2-line"></i>
                        <span class="ml-1">Shop</span>
                        <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-blue-600 group-hover:w-full transition-all duration-300"></span>
                    </a>
                    <a href="/cart" class="relative text-gray-700 hover:text-blue-600 transition-all duration-300 group">
                        <i class="ri-shopping-cart-2-line"></i>
                        <span class="ml-1">Cart</span>
                        <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-blue-600 group-hover:w-full transition-all duration-300"></span>
                    </a>
                    <a href="/users/logout" class="relative px-3 py-1 rounded-full text-red-600 hover:text-white hover:bg-red-600 transition-all duration-300 group border border-red-200 hover:border-red-600">
                        <i class="ri-logout-box-r-line"></i>
                        <span class="ml-1">Logout</span>
                        <span class="absolute inset-0 rounded-full bg-red-600 opacity-0 group-hover:opacity-10 transition-opacity duration-300"></span>
                    </a>
                </div>
            <% } %>
        </nav>
    <% } %>

</body>
</html> -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    keyframes: {
                        'fade-in': {
                            '0%': { opacity: '0', transform: 'translateY(-10px)' },
                            '100%': { opacity: '1', transform: 'translateY(0)' }
                        },
                        'pulse-glow': {
                            '0%, 100%': { transform: 'scale(1)', opacity: '1' },
                            '50%': { transform: 'scale(1.05)', opacity: '0.8' }
                        }
                    },
                    animation: {
                        'fade-in': 'fade-in 0.5s ease-out',
                        'pulse-glow': 'pulse-glow 3s ease-in-out infinite'
                    }
                }
            }
        }
    </script>
</head>

<body class="font-sans bg-gray-100 text-gray-900">
  
    <% if (typeof showHeader === "undefined" || showHeader) { %>
        <nav class="w-full flex items-center justify-between px-4 sm:px-6 py-3 sm:py-4 bg-gradient-to-r from-white to-blue-50 shadow-sm sticky top-0 z-50 backdrop-blur-sm border-b border-blue-100 animate-fade-in">
            <div class="flex items-center space-x-2 group">
                <div class="relative">
                    <div class="absolute -inset-1 bg-blue-200 rounded-full blur opacity-75 group-hover:opacity-100 transition-opacity duration-300"></div>
                    <svg class="w-6 h-6 sm:w-8 sm:h-8 text-blue-600 relative transform group-hover:rotate-12 transition-transform duration-300" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 0l-3 3a1 1 0 001.414 1.414L9 9.414V13a1 1 0 102 0V9.414l1.293 1.293a1 1 0 001.414-1.414z" clip-rule="evenodd"></path>
                    </svg>
                </div>
                <h3 class="text-xl sm:text-2xl font-bold bg-gradient-to-r from-blue-600 to-blue-400 bg-clip-text text-transparent animate-pulse-glow">Scatch</h3>
            </div>
        
            <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
            <% if (loggedin) { %>
                <div class="flex items-center gap-2 sm:gap-6">
                    <a href="/shop" class="relative text-gray-700 hover:text-blue-600 transition-all duration-300 group text-sm sm:text-base">
                        <i class="ri-store-2-line"></i>
                        <span class="hidden sm:inline ml-1">Shop</span>
                    </a>
                    <a href="/cart" class="relative text-gray-700 hover:text-blue-600 transition-all duration-300 group text-sm sm:text-base">
                        <i class="ri-shopping-cart-2-line"></i>
                        <span class="hidden sm:inline ml-1">Cart</span>
                    </a>
                    <a href="/users/logout" class="relative px-2 sm:px-3 py-1 rounded-full text-red-600 hover:text-white hover:bg-red-600 transition-all duration-300 group border border-red-200 hover:border-red-600 text-sm sm:text-base">
                        <i class="ri-logout-box-r-line"></i>
                        <span class="hidden sm:inline ml-1">Logout</span>
                        <span class="absolute inset-0 rounded-full bg-red-600 opacity-0 group-hover:opacity-10 transition-opacity duration-300"></span>
                    </a>
                </div>
            <% } %>
        </nav>
    <% } %>

</body>
</html>